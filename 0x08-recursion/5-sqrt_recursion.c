#include <stdio.h>
#include "main.h"

/**
 * _sqrt_recursion - fgdfgdf
 * @n: fgfg
 * @val:gyyt
 * Return: hghfg
*/
int sqrt(int n, val);
int _sqrt_recursion(int n)
{
	return (sqrt(n, 1));

}

/**
 * sqrt - jjhgh
 * @n: nhgvh
 * @val: hbjhb
 * Return: gjf
*/

int sqrt(int n, val)
{
	if (val *val == n)
		return (val);
	else if (val *val < n)
		return (sqrt(n, val + 1));
	else
		return (-1);
}
