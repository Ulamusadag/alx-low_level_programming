#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all except
 *
 * Return: 0 (success)
*/

int main(void)
{
	char ch = 'a';

	while (a <= 'z')
	{
		if (ch == 'e' || ch == 'q')
			ch++;
		putchar(char);
		char++;
	}

	putchar('\n');
	return (0);

}
