#include <stdio.h>

/*
 * main - End point
 *
 * Description: ghfdfd
 *
 * Return: 0 (success)
 *
*/

int main(void)
{

	char ch;
        for(ch = 'a', ch >=0, ch++)
	{
		putchar(ch);
	}
	putchar(\n);
	return (0);
}
